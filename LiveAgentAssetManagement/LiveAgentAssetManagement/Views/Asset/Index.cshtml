@model IEnumerable<LiveAgentAssetManagement.Entity.AssetModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<p></p>
<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>




<link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.15/css/dataTables.bootstrap.min.css">



<div class="panel panel-default">
    <div class="panel-heading">Asset List</div>
    <div class="panel-body">
        <table class="table table-striped table-bordered" id="assets">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.AssetName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AssetSrNo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DepartmentName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CategoryName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BrandName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Supplier)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PurchasedDate)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AssetName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AssetSrNo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DepartmentName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CategoryName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BrandName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Supplier)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PurchasedDate)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.AssetId }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.AssetId }) |
                            <span onclick='generateBarcode(this)' id="@item.AssetSrNo" title="Print Barcode" class="glyphicon glyphicon-barcode"></span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<div style="display: none" id="printDiv">
    <img id="barcode" alt="" />
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.10.15/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.15/js/dataTables.bootstrap.min.js"></script>
    <script src="../../Scripts/Barcode/JsBarcode.js"></script>
    <script src="../../Scripts/Barcode/CODE128.js"></script>
    <script type="text/javascript" charset="utf8">
        $(document).ready(function () {
            $('#assets').DataTable({ processing: true });
        });

        function generateBarcode(barcodeSpan) {
            $("#barcode").JsBarcode(barcodeSpan.id, {
                width: 2,
                height: 100,
                quite: 10,
                format: "CODE128",
                backgroundColor: "#fff",
                lineColor: "#000",
                displayValue: false
            });
            printDiv();
        }

        function printDiv() 
        {

            var divToPrint = document.getElementById('printDiv');

            var newWin=window.open('','Print-Window');

            newWin.document.open();

            newWin.document.write('<html><body onload="window.print()">'+divToPrint.innerHTML+'</body></html>');

            newWin.document.close();

            setTimeout(function(){newWin.close();},10);

        }
    </script>
}


